  <div class="favorites-title">
    <div class="favorites-title-col">
      <%= image_tag('bookmark-alone-purple.svg') %>
      <span>Thèmes <%= scope %></span>
    </div>
    <% if add_arrow %>
      <%= link_to  image_tag('arrow-double-fat.svg'), arrowthemepath %>
    <% end %>
  </div>
 <div class="micro-themes-list">
  <% themes.each do |theme| %>
    <a href="<%= theme_path(theme) %>">
      <div class="micro-theme-card">
        <div class="micro-theme-card-img">
          <% if (theme.picture.attached?) %>
              <%= cl_image_tag(theme.picture.key) %>
            <% else %>
              <%= image_tag('frontlorem.jpg') %>
            <% end %>
        </div>
        <span><%= theme.title %></span>
        <div class="counter">
          <%= image_tag('eye.svg') %>
          <%= Favorite.where(favoritable_id: theme.id).count %>
        </div>
      </div>
    </a>
  <% end %>
</div>
