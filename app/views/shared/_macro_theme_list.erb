  <div class="favorites-title themes-title-all">
    <div class="favorites-title-col">
      <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="#902CB8" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="feather feather-compass"><circle cx="12" cy="12" r="10"></circle><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76"></polygon></svg>
      <span>Tous les thèmes<%= scope %></span>
    </div>
    <% if add_arrow %>
      <%= link_to  image_tag('arrow-double-fat.svg'), arrowthemepath %>
    <% end %>
  </div>
 <div class="macro-themes-list">
  <% themes.each do |theme| %>
    <a href="<%= theme_path(theme) %>">
      <div class="macro-theme-card" style="background: url(
        <% if (theme.picture.attached?) %>
            <%= cl_image_path(theme.picture.key) %>
          <% else %>
            <%= image_path('frontlorem.jpg') %>
          <% end %>
            ); background-repeat: no-repeat; background-size: 100% 50%; background-position: 50% 0%;">
        <span class="font-title"><%= theme.title %></span>
        <div class="counter-macro">
          <%= image_tag('eye.svg') %>
          <%= Favorite.where(favoritable_id: theme.id).count %>
        </div>
      </div>
    </a>
  <% end %>
</div>
